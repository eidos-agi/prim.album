# prim.album

One record as a Prim. Tracks and metadata. The player is a view.

```bash
python3 validate.py examples/siliconfall-score
```

See [SPEC.md](SPEC.md). Not `prim.deck` (slides). Not `prim.arcade` (a cart). Do not mint `prim.track`.

MIT — Eidos AGI
